import React from "react";

class MyButton extends React.Component {
    render() {
        return (
            <button>
                Botão
            </button>
        )
    }
}

export default MyButton;